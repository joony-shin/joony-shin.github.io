# Hugo
/public/
/resources/_gen/
/.hugo_build.lock
hugo_stats.json

# OS / editor
.DS_Store
*.swp

# automation
automation/.env
__pycache__/
# automation/state.json, automation/track_state.json 은 실행 간 중복-발행·중복-채점
# 방지 상태를 유지해야 하므로 의도적으로 추적한다(.gitignore 에서 제외).

automation/logs/
